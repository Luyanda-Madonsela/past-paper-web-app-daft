export const metadata = {
    title: 'Find Questions',
    description: 'Past paper web application',
}

export default function FindSpecificQuestions({ children }) {
    return (
        <div>
            {children}
        </div>  
    )
    }