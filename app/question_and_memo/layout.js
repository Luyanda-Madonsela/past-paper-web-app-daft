export const metadata = {
    title: 'Questions and Memo',
    description: 'Past paper web application',
}

export default function Questions_Memo({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }