export const metadata = {
    title: 'Sign In',
    description: 'Past paper web application',
}

export default function SignIn({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }