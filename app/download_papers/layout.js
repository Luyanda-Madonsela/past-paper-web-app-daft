export const metadata = {
    title: 'Download Papers',
    description: 'Past paper web application',
}

export default function DownloadPapers({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }