export const metadata = {
    title: 'About Us',
    description: 'Past paper web application',
}

export default function About({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }