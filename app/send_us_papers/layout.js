export const metadata = {
    title: 'Send Us Papers',
    description: 'Past paper web application',
}

export default function SendUsPapers({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }