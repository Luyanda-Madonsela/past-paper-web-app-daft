export const metadata = {
    title: 'Contact Us',
    description: 'Past paper web application',
}

export default function Contact({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }