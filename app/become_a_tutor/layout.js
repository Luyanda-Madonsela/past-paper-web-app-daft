export const metadata = {
    title: 'Become A Tutor',
    description: 'Past paper web application',
}

export default function BecomeATutor({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }