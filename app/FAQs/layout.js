export const metadata = {
    title: 'FAQs',
    description: 'Past paper web application',
}

export default function FAQs({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }