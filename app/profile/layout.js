export const metadata = {
    title: 'Profile',
    description: 'Past paper web application',
}

export default function Profile({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }