export const metadata = {
    title: 'Get A Tutor',
    description: 'Past paper web application',
}

export default function GetATutor({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }