export const metadata = {
    title: 'Donate',
    description: 'Past paper web application',
}

export default function Donate({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }