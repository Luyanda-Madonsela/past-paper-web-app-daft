export const metadata = {
    title: 'Terms and Conditions',
    description: 'Past paper web application',
}

export default function TermsAndConditions({ children }) {
    return (
        <div>
            {children}
        </div>  
    )
    }