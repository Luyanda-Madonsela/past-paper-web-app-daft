export const metadata = {
    title: 'Cookies Policy',
    description: 'Past paper web application',
}

export default function CookiesPolicy({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }