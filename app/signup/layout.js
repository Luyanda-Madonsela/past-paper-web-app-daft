export const metadata = {
    title: 'Sign Up',
    description: 'Past paper web application',
}

export default function SignUp({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }