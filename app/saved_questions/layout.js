export const metadata = {
    title: 'My Saved Questions',
    description: 'Past paper web application',
}

export default function SavedQuestions({ children }) {
    return (
        <div>
            {children}
        </div>
    )
    }